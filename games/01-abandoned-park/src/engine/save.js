// TODO: Implement localStorage profile-slot save system.
// See ABANDONED_PARK_PLAN.md → "TECH STACK" (Save: localStorage with profile
// slots) and "SHAREABILITY → Phase 2" (profile = name + saved progress:
// cleared stands, tickets, found clues, current night). World state and
// player state stay separated so multiplayer can layer on later without a
// rewrite. Sloane's existing "Profile button" design is the entry point.

export const Save = {};
