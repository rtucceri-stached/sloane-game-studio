# Normalize line endings to LF for all text files (prettier expects LF per .prettierrc.json)
* text=auto eol=lf

# Git LFS for Blender source files (overrides text=auto above)
*.blend filter=lfs diff=lfs merge=lfs -text
